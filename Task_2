#include <conio.h>
#include <stdio.h>
#include <math.h>

void main ()
{
	double v, x, y, z;
	x= 3.74e-2;
	y= -0.825;
	z= 0.16e2;
	v= (1+ pow(sin(x+y),2)/ fabs(x- (2*y)/ (1+ ((x*x)*(y*y)))))*pow(x,fabs(y)) + pow(cos(atan(1./z)),2);
	printf("\n result is: v= %lf", v);
	getch();

}
